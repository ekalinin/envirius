. $(get_command_full_path)/deactivate

mem_cmd_desc() {
    echo "Deactivate environment (alias: deactivate)"
}

mem_cmd_help() {
    show_usage
    show_desc "$(mem_cmd_desc)"
}

# /* vim: set filetype=sh : */
