cmd_default() {
    echo "Creating environment: $1 ..."
    mkdir -p $UVE_ENVS_DIR/$1/bin
}

cmd_desc() {
    echo "creates a new environment"
}

cmd_help() {
    show_usage "mk <env-name>"
    show_desc "$(cmd_desc)"
    exit 0
}

# /* vim: set filetype=sh : */
