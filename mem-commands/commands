mem_cmd_default() {
    # lists all files in the commands directory
    # except hidden and common file
    for cmd in $(find $MEM_COMMANDS_DIR \
                    -type f \
                    -not -iname "common" \
                    -not -iname ".*" | sort)
    do
        # source each command
        . $cmd
        # display it's filename only & description for it
        echo "  `rpad $(filename $cmd) 15` $(mem_cmd_desc)"
    done
}

mem_cmd_desc() {
    echo "list available commands"
}

mem_cmd_help() {
    show_usage
    show_desc "$(mem_cmd_desc)"
}

# /* vim: set filetype=sh : */
